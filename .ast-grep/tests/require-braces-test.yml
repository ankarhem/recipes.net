id: require-braces
valid:
  - if (x) { DoSomething(); }
  - |
    if (x) {
      DoSomething();
    } else {
      DoOther();
    }
  - if (condition) { }
  - |
    if (a) {
      if (b) {
        DoSomething();
      }
    }
  - |
    if (x == 1) {
      Foo();
    } else if (x == 2) {
      Bar();
    } else {
      Baz();
    }
  - |
    if (guard) {
      return;
    }
  - if (x > 0) { return true; }
  - for (var i = 0; i < n; i++) { DoSomething(); }
  - foreach (var x in items) { Process(x); }
  - while (condition) { DoWork(); }
  - |
    for (var i = 0; i < n; i++) {
      DoSomething();
    }
  - |
    foreach (var item in collection) {
      Process(item);
    }
  - |
    while (true) {
      DoWork();
    }
invalid:
  - if (x) DoSomething();
  - if (guard) return;
  - if (condition) Foo();
  - |
    if (x) DoSomething();
    else { DoOther(); }
  - |
    if (x) { DoSomething(); }
    else DoOther();
  - |
    if (x) DoSomething();
    else DoOther();
  - |
    if (a) {
      if (b) DoSomething();
    }
  - if (broken) throw new Exception();
  - if (flag) x = 42;
  - for (var i = 0; i < n; i++) DoSomething();
  - foreach (var x in items) Process(x);
  - while (condition) DoWork();
  - |
    for (var i = 0; i < n; i++)
      Console.WriteLine(i);
  - while (true) DoWork();
